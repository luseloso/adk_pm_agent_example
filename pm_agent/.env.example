# Choose Model Backend: 0 -> ML Dev (Gemini API), 1 -> Vertex AI
GOOGLE_GENAI_USE_VERTEXAI=1

# Google Cloud Configuration (for Vertex AI)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_CLOUD_STAGING_BUCKET=gs://your-staging-bucket

# MCP Server URL (set after deploying prd_mcp_server to Cloud Run)
# This is required for the agent to store and retrieve PRDs
MCP_SERVER_URL=https://your-mcp-server-url.run.app

# Optional: Gemini API Key (for ML Dev / Express Mode)
# GOOGLE_API_KEY=your-api-key
